//piedra papel o tijeras
#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;
int main()
{
    srand(time(0));
    int computadora;
    int usuario;
    computadora=rand()% 3+1;
    cout<<"piedra(1), papel (2), tijera (3)"<<endl,
    cout<<"elija uno: ";
    cin>>usuario;
    cout<<"la computadora eligio:"<<computadora<<endl;
    if(usuario ==computadora){
        cout<<"empate"<<endl;}
        else if ((usuario==1 and computadora==3) or (usuario==2 and computadora==1) or (usuario==3 and computadora==2)){
            cout<<"ganaste =)"<<endl; }
            else if ((usuario==3 and computadora==1) or (usuario==1 and computadora==2) or (usuario==2 and computadora==3)){
                cout<<"perdiste =("<<endl;
            }
 return 0;
    
}  